// This file probably needs a better name
const post_button = document.getElementById("upload_post_button");

// Attributes
//TODO constant document references

post_button.addEventListener("click", function(e) {
    getPostAttributes();
});

function getPostAttributes() {
    //TODO get values from document references
    compileHtml();
};

function compileHtml() {
    //TODO form values into html document
};

function postHtml() {
    //TODO identify images also needing sending

    //TODO send html doc

    /*
    .//TODO send image folder

    for each in folder:
        send folder[each];
    */
};